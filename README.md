# Skills-Master

Веб-сервис для генерации структурированного `skills.json` из CSV с компетенциями.

## 🚀 Возможности

- **Загрузка CSV** — парсинг CSV с автоопределением разделителя
- **Генерация через LLM** — использование моделей через OpenRouter API
- **Интерактивный UI** — аккордеоны с детальной информацией по каждому скиллу
- **Профиль специалиста** — генерация полного профиля с зарплатами, вопросами для собеседования
- **Скачивание JSON** — экспорт готового `skills.json`
- **Видео-тизеры** — генерация 1-минутных видео для каждого скилла через HeyGen AI

## 📋 Формат CSV

```csv
Формулировка компетенции (проект),A
Способен осуществлять предварительный анализ данных,X
Способен применять знания об истории развития ИИ,
Способен разрабатывать алгоритмы обработки языка,X
```

- **Первая колонка** — текст компетенции/скилла
- **Вторая колонка** — маркер `X` для включения в профиль
- Первая строка — заголовок (пропускается)

## 🛠 Технологии

- **Next.js 14** (App Router)
- **TypeScript**
- **Tailwind CSS**
- **Zod** для валидации
- **OpenRouter API** для LLM

## 🔧 Установка

```bash
# Клонирование
git clone <repo-url>
cd skills-master

# Установка зависимостей
npm install

# Настройка переменных окружения
cp .env.example .env.local
# Добавьте OPENROUTER_API_KEY в .env.local

# Запуск dev-сервера
npm run dev
```

Откройте [http://localhost:3000/admin](http://localhost:3000/admin)

## 🔑 Переменные окружения

| Переменная | Описание |
|------------|----------|
| `OPENROUTER_API_KEY` | API ключ OpenRouter (обязательно) |
| `HEYGEN_API_KEY` | API ключ HeyGen для видео-тизеров (опционально) |
| `ELEVENLABS_API_KEY` | API ключ ElevenLabs для аудио-подкастов (опционально) |

### Медиа-тизеры (Audio + Video)

Приложение поддерживает генерацию мультимедийного контента для каждого навыка:

- **Аудио-подкаст** (ElevenLabs) — мгновенный результат за ~3-5 секунд
- **Видео-тизер** (HeyGen) — высококачественное видео с аватаром за ~60-120 секунд

### Аудио-подкасты (ElevenLabs) — РЕКОМЕНДУЕТСЯ

Быстрая генерация аудио с естественным русским произношением:

1. Получите API ключ на [elevenlabs.io](https://elevenlabs.io)
2. Добавьте в `.env.local`:
   ```
   ELEVENLABS_API_KEY=your_key_here
   ```

**Преимущества:**
- ⚡ Генерация за 3-5 секунд
- 🎙️ Высокое качество голоса (модель `eleven_multilingual_v2`)
- 💰 Дешевле чем видео

### Видео-тизеры (HeyGen)

Для генерации 1-минутных видео-тизеров навыков:

1. Получите API ключ на [heygen.com](https://heygen.com)
2. Добавьте в `.env.local`:
   ```
   HEYGEN_API_KEY=your_key_here
   ```

#### Использование
1. Сгенерируйте скиллы из CSV
2. Раскройте любой аккордеон скилла
3. Нажмите «Сгенерировать 1-мин тизер»
4. Дождитесь генерации сценария (~10 сек)
5. Дождитесь рендеринга видео (~60-120 сек)
6. Видеоплеер появится автоматически

#### Troubleshooting
- **Видео застряло на обработке**: HeyGen может быть под нагрузкой, подождите или повторите
- **Ошибка авторизации**: Проверьте валидность HEYGEN_API_KEY
- **Превышен лимит**: Подождите несколько минут перед повтором

## 📦 Деплой на Vercel

1. Подключите репозиторий к Vercel
2. Добавьте `OPENROUTER_API_KEY` в Environment Variables
3. Deploy!

```bash
# Или через CLI
vercel --prod
```

## 📁 Структура проекта

```
src/
├── app/
│   ├── admin/page.tsx        # Главная страница админки
│   ├── api/
│   │   ├── generate/         # API эндпоинт генерации скиллов
│   │   ├── audio/            # API эндпоинты для аудио
│   │   │   └── create/       # POST: генерация аудио (ElevenLabs)
│   │   └── heygen/           # API эндпоинты для видео
│   │       ├── create/       # POST: создание видео (HeyGen)
│   │       └── status/       # GET: статус генерации
│   ├── layout.tsx
│   └── page.tsx
├── components/
│   ├── file-upload.tsx       # Загрузка CSV
│   ├── model-selector.tsx    # Выбор модели LLM
│   ├── skill-accordion.tsx   # Аккордеон скилла
│   ├── media-teaser.tsx      # Компонент медиа-тизера (audio + video)
│   ├── profile-accordion.tsx
│   └── ...
├── lib/
│   ├── csv-parser.ts         # Парсер CSV
│   ├── openrouter.ts         # Клиент OpenRouter
│   ├── skill-generator.ts    # Генератор скиллов
│   ├── script-generator.ts   # Генератор сценариев для видео
│   ├── heygen.ts             # Клиент HeyGen API (видео)
│   ├── elevenlabs.ts         # Клиент ElevenLabs API (аудио)
│   ├── profile-builder.ts    # Построитель профиля
│   └── prompts/              # Промпты для LLM
├── types/
│   ├── skills.ts             # TypeScript типы скиллов
│   ├── heygen.ts             # TypeScript типы HeyGen
│   └── csv.ts
└── hooks/
    ├── useGeneration.ts      # Хук генерации скиллов
    └── useMediaGenerator.ts  # Хук генерации медиа (audio + video)
```

## 📊 Выходной формат

```typescript
interface SkillsJson {
  meta: {
    profileId: string;
    profileName: string;
    sourceFile: string;
    generatedAt: string;
    model: string;
  };
  skills: Skill[];
  specialistProfile: SpecialistProfile;
}
```

## 🎯 Roadmap

- [ ] Поддержка нескольких колонок профилей (A/B/C)
- [ ] Редактирование скиллов в UI
- [ ] История генераций
- [ ] Экспорт в Markdown/HTML

## 📝 License

MIT
